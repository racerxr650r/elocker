{#-
  Plan.md.j2 — Schema-driven retrofit demo template.

  This template is the smoke proof for the Phase 2.5 schema-driven
  contract: it was added with no TypeScript edits to the VS Code
  extension. Discovery happens entirely via:

    1. A `<plan>` payload defined in tools/project.xsd
       (Plan complexType, mixed content + optional <item> children).
    2. A `<metadata><document id="Plan" template="..." output="..."/>`
       entry that the sidecar's `list_documents` method enumerates.
    3. This Jinja2 template, picked up by render_doc.py via the
       conventional path `tools/templates/Plan.md.j2`.

  The `project` namespace is exposed by render_doc.py exactly as for
  the other generated documents; this template only needs the
  metadata block, so it stays small.
-#}
# {{ project.metadata.title }}

> Project: **{{ project.name }}** ({{ project.short_name }})
> Version: {{ project.metadata.version }} — {{ project.metadata.date }}
> Author:  {{ project.metadata.author }}

This document is generated by `render_doc.py` from `Project.xml`
(payload `<plan>`). It exists to prove the Phase 2.5 schema-driven
contract: the VS Code extension picks up this document — its tree
node, its `projectXml.render.Plan` command, and its Markdown preview
target — entirely from the `<metadata><document id="Plan">` entry,
with **zero TypeScript changes**.

## Plan items
