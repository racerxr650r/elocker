/* A project header. Its function belongs to this file and to no file that
 * includes it — counting it once per includer would inflate every figure by
 * the inclusion count (HLR-203). */
static int helper_from_header(int x)
{
	return x + 1;
}
